# Job-Postings-Data-Analysis
ML project for analyzing job postings data. Pred. Tuned models: Logistic Regression, LinearSVM, Decision Tree, Random Forest, XGBoost, CatBoost. Evaluated via Accuracy, Precision, Recall, F1, ROC/PR AUC; curves + feature importances; best model selected.
